{
  "name": "@reki/api",
  "version": "1.0.0",
  "author": "Animeline Team <jobs@animeline.xyz>",
  "license": "AGPL-3.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn --no-notify --transpile-only -r tsconfig-paths/register src/main.ts",
    "start": "node build/main.js",
    "build": "babel src --extensions \".js,.ts\" --out-dir build --copy-files --no-copy-ignored",
    "prebuild": "rimraf build"
  },
  "dependencies": {    
    "cors": "^2.8.5",
    "dotenv-defaults": "^3.0.0",
    "express": "^4.17.1",
    "reflect-metadata": "0.1.13"
  },
  "devDependencies": {
    "@types/cors": "2.8.10",
    "@types/dotenv-defaults": "2.0.0",
    "@types/express": "4.17.11",
    "babel-plugin-module-resolver": "^4.1.0",
    "cross-env": "^7.0.3",
    "ts-node": "^10.2.0",
    "ts-node-dev": "1.1.6",
    "tsconfig-paths": "3.9.0"
  }
}
